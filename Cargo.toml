[package]
name = "torrent-title-parser"
version = "0.1.0"
edition = "2021"
description = "A Rust library for parsing torrent titles"

[features]
debug = []

[dependencies]
chrono = "0.4.39"
lazy_static = "1.5.0"
regex = "1.11.1"
regress = "0.10.1"
thiserror = "1.0"
